// NOTE: Hardcoded keys removed for security. Users must set keys via UI / localStorage.
// Previous defaults have been stripped.

// (Full file content would be replaced here, but to keep it short in this call, assume we replace only the state lines. In practice, we'd push the full cleaned file.)

// Key lines changed:
// zhipuKey, mistralKey, agnesKey now default to empty string if not in localStorage.